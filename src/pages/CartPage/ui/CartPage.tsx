import {Page} from '@/widgets/Page';
import {Cart} from "@/entities/CartItem";

const CartPage = () => {
    return (
        <Page>
            <Cart />
        </Page>
    );
};

export default CartPage;